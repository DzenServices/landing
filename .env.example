# Public base site URL used for SEO canonical/alternates and JSON-LD
NEXT_PUBLIC_SITE_URL=https://example.com

# If you run locally with Docker directly (optional)
# PORT mapping is defined in docker-compose.yml (host 80 -> container 3000)
